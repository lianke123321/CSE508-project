To compile the program properly, you need to install pcre library first:
	$ sudo apt-get install libpcre3-dev

To run quantumdetect.py properly, you need to install scapy library first.
Please follow the instruction in this website:
	http://www.secdev.org/projects/scapy/doc/installation.html

Please make first to compile the source code.

quantuminject usage:
	$ sudo ./quantuminject.o -i <interface> -r <regex> <optional filter>
	Or
	$ sudo ./quantuminject.o -r <regex> -d <filename> <optional filter>
	OR
	$ sudo ./quantuminject.o -r <regex> <optional filter>

quantumdetect usage:
	$ sudo python quantumdetect.py -i <interface> <optional filter>
	OR
	$ sudo python quantumdetect.py -r <tracefile> <optional filter>
	OR
	$ sudo python quantumdetect.py <optional filter>